"use client"
import { useState,useEffect } from "react";


    
export default function Fetching({id}){
 return <>
{/* {const [userInfo, setUserInfo] = useState(null);
    

    useEffect(() => {
        

        
        console.log(router, identifier);

        if (identifier) {
            // Define async function to fetch user data
            async function getUserId(identifier) {
             
                    const data = await fetch(`https://dummyjson.com/users/${identifier}`);
                    if (!data.ok) {
                        throw new Error("Failed to fetch user data");
                    }
                    const userData = await data.json();
                    setUserInfo(userData); // Update the state with user data
                
            }

            // Fetch user data
            getUserId(identifier);
        }
    }, [identifier]);} */}
 </>
}